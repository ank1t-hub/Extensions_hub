# Docker MySQL (used by docker-compose.yml) — Phase 0.2
# Host port; must match port in backend/.env DATABASE_URL
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=extensionhub
MYSQL_USER=extensionhub
MYSQL_PASSWORD=extensionhub
